# نقشهٔ راه — از پروژه‌ی شخصی تا AAA
### «دژِ ابدیت / Eternity Defense»

**آخرین به‌روزرسانی:** هر مرحله که تمام شود، تیک می‌خورد و پوش می‌شود.
**نمرهٔ شروع:** ۵.۴ / ۱۰ · **هدفِ این دور:** ۹+ / ۱۰ · ** هدفِ نهایی:** AAA

---

## فاز ۰ — ثبات و ایمنی (بقا) ✅
بدون این‌ها بازی «قابلِ انتشار» نیست.

- [x] **۰.۱** رفعِ فریزِ قطعی: شعاعِ منفی در `ctx.ellipse` (دشمنِ «سایه») — `renderEnemies.ts:۲۸۹`
- [x] **۰.۲** ایمن‌سازیِ دو الگوی مشابه: ملکهٔ مه (`render.ts:۱۲۴۴`) و پادشاه (`renderEnemies.ts:۶۵۴`)
- [x] **۰.۳** افزودنِ کلَمپِ سراسریِ `safeRadius()` برای تمام بیضی‌ها و دایره‌های رندر
- [x] **۰.۴** `try/catch` دورِ حلقهٔ `requestAnimationFrame` — هیچ خطای رندری دیگر بازی را نمی‌کشد
- [x] **۰.۵** `ErrorBoundary` در React — سقوطِ یک کامپونent به جای صفحهٔ سیاه
- [x] **۰.۶** RNGِ بذردار و تعیین‌پذیر (`src/game/rng.ts`) جای `Math.random` در موتور
- [x] **۰.۷** ذخیره و بازیابیِ **دورِ در حال اجرا** (مهم برای موبایل: تماس، بک‌گراند، چرخش صفحه)

## فاز ۱ — بهداشتِ پروژه 🟡
- [x] **۱.۱** `.gitignore` (جلوگیری از کامیتِ `node_modules` و `dist`)
- [x] **۱.۲** لایسنس
- [x] **۱.۳** CI در گیت‌هاب اکشنز (typecheck + build روی هر push/PR)
- [x] **۱.۴** رفعِ ادعای غلطِ README دربارهٔ «فایلِ خودبسنده»
- [x] **۱.۵** بیلدِ **واقعاً** خودبسنده (درون‌خط‌سازیِ تصویرها در `index.html`)
- [x] **۱.۶** تست‌های خودکار روی موتور (`vitest`)
- [x] **۱.۷** تستِ تعادلِ خودکار — جلوگیری از تکرارِ فاجعهٔ «کمپینِ بی‌درد»

## فاز ۲ — تعادل و اقتصاد 🟡
- [x] **۲.۱** افزایشِ فشارِ جانِ دشمنان در کمپین
- [x] **۲.۲** کاهشِ درآمدِ طلا و سودِ بانکی
- [x] **۲.۳** مخزنِ مصرفِ طلایِ بی‌انتها (فرااوجِ نامحدود با هزینهٔ نمایی)
- [x] **۲.۴** بازتست با شبیه‌ساز تا رسیدن به منحنیِ هدف
- [x] **۲.۵** تنظیمِ دوباره‌یِ باس‌ها و زره متناسب با منحنیِ جدید

## فاز ۳ — موبایل، لنداسکیپ و UX ✅
- [x] **۳.۱** چیدمانِ اختصاصیِ **Landscape** (نقشه + کنترل‌ها کنار هم، نه روی هم)
- [x] **۳.۲** پشتیبانی از تمامِ اندازه‌ها: از ۴.۷ اینچ تا تبلت و آیفون‌های دارای ناچ
- [x] **۳.۳** امنیتِ ناحیهٔ امن (`safe-area-inset`) در هر چهار جهت
- [x] **۳.۴** اهدافِ لمسی ≥ ۴۴ پیکسل و تایپوگرافیِ مقیاس‌پذیر (`clamp`)
- [x] **۳.۵** جلوگیری از اسکرول/زومِ ناخواسته‌یِ مرورگر، pull-to-refresh و دابل‌تپ
- [x] **۳.۶** حالتِ تمام‌صفحه و قفلِ جهت به لنداسکیپ روی موبایل
- [x] **۳.۷** بازخوردِ لمسی (Haptic) در لحظه‌های مهم

## فاز ۴ — مسیرِ AAA (انجام شد، تا سقفِ ممکنِ بدونِ بازنویسیِ WebGL) ✅

### ۴.۱ هنر و صدا
- [x] **صداگذاریِ کاملِ همه‌یِ دیالوگ‌ها به سبکِ Undertale** — هر شخصیت سازِ خودش را دارد:
  نیبو (مربعِ روشن و جهنده)، نیبوی خسته (مثلثِ گرم با دِتون)، پادشاهِ خاموشی (اره‌ایِ بم + غرشِ نویزی)،
  راوی (نجوای سینوسی با هوا)، قلب (زنگِ نقره‌ای با هارمونیک)، سالگرد (مثلثِ کهنه با نفس).
  صدا در: دیالوگ‌ها، پیشامدها، پایان‌بندی‌ها و صفحهٔ پایانِ بازی. — تست‌شده در مرورگر، صفر خطا
- [x] **سیستمِ قلمِ فارسیِ چندلحنی (self-hosted، بدون CDN، آفلاین):**
  وزیرمتن (رابط) · لاله‌زار (تیتر و کلمات کوبنده) · مرکزی (گفت‌وگوی گرم) · نسخ (حکایت) · نستعلیق (شکوهِ پادشاه و بنر فصل)
  — ۱۸ فایل woff2، همگی داخلِ بیلدِ تک‌فایلی inlined
- [ ] بازسازیِ تمامِ تصویرها با یک سبکِ واحدِ دستی (الآن ترکیبی از تصویرهای AIست)
- [ ] اسپرایت‌شیت و انیمیشنِ فریم‌به‌فریم به‌جای رندرِ رویه‌ای
- [ ] شیدرهای پس‌پردازش (bloom، CRT، depth-of-field) در WebGL
- [x] **موسیقیِ لایه‌ایِ واکنش‌گرا** — تراک با شدتِ نبرد نفس می‌کشد: بیس همیشه؛ اسنیر از حرارتِ ۰.۳۵؛ های‌هت از ۰.۵۵؛ آکتاو از ۰.۸؛ باس = تمامِ لایه‌ها. حرارت از تراکمِ دشمنانِ زنده + حضورِ باس محاسبه می‌شود

### ۴.۲ گیم‌پلیِ عمیق‌تر
> **فلسفهٔ ثابت:** فول‌تاچ — همه‌چیز با لمس کامل می‌شود؛ هیچ ابزارِ جانبی (گیم‌پد و…) لازم نیست. همه‌چیز **آفلاین و محلی** است؛ هیچ قابلیت آنلاینی راه نمی‌آید.
- [x] **درختِ مهارتِ معمار** — «ستون‌های معمار» در آیینهٔ خاطرات: ۳ شاخه (خزانه‌داری/سنگ‌بنا/نبض) × ۲ سنگِ وابسته به هم، دائمی و با خرده‌خاطره
- [x] **چرخهٔ شبانه‌روز** — نورِ میدان هر ۱۰ موج سفر می‌کند: روز → غروب → شب → سپیده؛ موجِ باس شفاف؛ دوزخ همیشه ته‌رنگِ خونی (صرفاً بصری)
- [x] **مودِ «دوزخ»** — چالشِ اختیاری: دشمنان ×۱.۴۵ جان/طلا، قلب با نیمِ جان، روح‌ها ×۱.۵؛ با عبور از موج ۳۰ باز می‌شود؛ مدالِ «مارشِ دوزخ»
- [ ] ضبط و پخشِ ریپلیِ محلی (با RNGِ بذردار آماده است)

### ۴.۳ فنی
- [ ] انتقالِ رندر به WebGL و batchingِ draw-callها
- [x] **PWA با نصبِ آفلاین** — manifest + آیکون‌های ۱۹۲/۵۱۲ (تولیدشده در بیلد، بدون هیچ وابستگی) + سرویس‌ورکرِ cache-first. تست‌شده: بعد از اولین بازدید، بازی با اینترنتِ کامل قطع اجرا می‌شود؛ نصبِ «Add to Home Screen» روی موبایل با قفلِ لنداسکیپ

### ۴.۴ محتوا
- [ ] ۱۲ فصل به‌جای ۶ (دو چرخه‌یِ کاملِ داستانی)
- [ ] پایان‌هایِ بیشتر بر اساسِ «مسیرِ بخشش/تاج»
- [ ] دشمنان و باس‌هایِ تازه برای هر فصل

> حذف‌شده از نقشهٔ راه به تصمیمِ سازنده: مخزنِ نقشه‌های چندگانه، چالشِ روزانه با seed مشترک،
> لیدربورد و Ghost، شبیه‌سازیِ سروری، تلمتری/مانیتورینگِ ابری، پشتیبانیِ گیم‌پد.

---

## فاز ۵ — بالانسِ نهایی (بعد از تمامِ تغییرات) ✅
- [x] **۱۸/۱۸ تستِ سبز** روی نسخهٔ نهایی — شامل ۴ guard-rail تعادل (کفِ سختی، فشارِ میانهٔ کمپین، پاداشِ مهارت، سقفِ بی‌پایان)
- [x] صداگذاری، قلم‌ها، موسیقیِ واکنش‌گرا و شب/روز هیچ اثری بر شبیه‌سازی ندارند (لایهٔ رندر/صدا جدا از موتور) — منحنیِ حالتِ عادی دست‌نخورده
- [x] دوزخ و ستون‌های معمار هر دو opt-in/daimi-اختیاری‌اند و کفِ سختی را جابه‌جا نمی‌کنند؛ ×۱.۴۵ دوزخ با تستِ سید-به-سید قفل شد
- [x] اسموکِ مرورگر روی همهٔ قابلیت‌های نو: دکمهٔ دوزخِ قفل/باز، نشانِ دوزخ در HUD، خریدِ ستون (۴۰→۳۶ خرده‌خاطره)، ۶ سنگِ درخت، صفر خطای کنسول

## وضعیتِ فعلی
| فاز | وضعیت |
|---|---|
| ۰ — ثبات | ✅ تمام شد — تست‌شده در مرورگر: فریز رفع، ذخیره/ادامه فعال، صفر خطا |
| ۱ — بهداشت | ✅ تمام شد — ۱۴ تستِ خودکار سبز، CI فعال، بیلدِ تک‌فایلیِ واقعی |
| ۲ — تعادل | ✅ تمام شد — منحنیِ جدید با شبیه‌ساز تنظیم و با تست قفل شد |
| ۳ — موبایل | ✅ تمام شد — تست‌شده روی iPhone SE/12 مجازی + تبلت |

### نتیجهٔ فاز ۳ (اندازه‌گیری‌شده در شبیه‌سازِ دستگاه)
| دستگاه | نوارِ بالا | داک | بورد | FPS |
|---|---|---|---|---|
| iPhone SE (۶۶۷×۳۷۵ افقی) | ۳۸px | ۶۵px | تمام‌عرض | ~۵۰ |
| iPhone 12 (۸۴۴×۳۹۰ افقی) | ۴۲px | ۷۵px | ۱۰۰٪ عرض | ~۵۰ |
| پرتره (۳۹۰×۸۴۴) | راهنمای «گوشی را افقی بگیر» + دکمهٔ تمام‌صفحه | | | |
| ۴ — AAA | ✅ صداگذاری · قلم‌ها · موسیقیِ واکنش‌گرا · ستون‌های معمار · دوزخ · شب/روز · PWA |
| ۵ — بالانس نهایی | ✅ ۱۸/۱۸ تستِ سبز (شامل ۴ guard-rail تعادل) |

### نتیجهٔ فاز ۲ (اندازه‌گیری‌شده، همان باتِ سه‌سطحی)

| پروفایل | قبل | بعد |
|---|---|---|
| خوب | کمپین بی‌درد؛ اولین آسیب موج ۱۶۰؛ مرگ ۲۱۵ | کمپین بی‌لک؛ مرگ در بی‌پایان موج ۸۹ |
| معمولی | اولین آسیب موج ۱۲۰؛ مرگ ۱۵۲ | اولین نشتی موج ۵۴؛ نبردِ نهایی روی باسِ موج ۶۰ |
| ضعیف | مرگ موج ۱۶ | مرگ موج ۱۶ (کفِ سختی حفظ شد) |
| طلا | ۳٬۲۵۴٬۷۵۴ بلااستفاده | ~۲۹۷٬۰۰۰ مصرف‌شده در «قدرتِ افسانه‌ای» |
